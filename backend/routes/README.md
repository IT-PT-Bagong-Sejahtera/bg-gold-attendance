# Routes

Registry route aktif berada di `controllers/server.go`. Kontrak route yang dapat dibaca mesin berada di `documentation/openapi.yaml`.

Pemisahan folder ini menjaga tempat untuk route module baru tanpa memaksa seluruh handler lama menjadi public API package.
