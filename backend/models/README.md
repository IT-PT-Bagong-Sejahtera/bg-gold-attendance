# Models

Model attendance berada dekat business rule-nya di `services/attendance/types.go`. Struktur request/response yang khusus endpoint berada dekat controller terkait. Folder ini disediakan untuk model lintas fitur ketika domain bertambah, sehingga tidak tercipta satu file model global yang saling bergantung.
